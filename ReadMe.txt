

Строка подключения настроивается в проекте APIService->appsettings.json

Порт для APIService в файле launchSettings.json(при его смене, необходисо также поменять в проекте Client->Helpers->valuesHelper)


